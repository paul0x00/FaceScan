# 提交记录

按时间顺序记录每一次提交的主要工作内容。

## 2026-06-09 feat: refresh point cloud workspace and dev tooling

- 点云查看页改为沉浸式工作区布局，收起左侧表单面板并新增悬浮工具栏。
- 增加纹理显示开关、六视图快捷切换、外部点击收起和底部操作提示，提升点云预览交互效率。
- 点云相机重置逻辑改为基于包围盒恢复前视图，并补充标准视角切换时的相机定位处理。
- 新增 `Makefile`、`scripts/dev.sh` 和 `scripts/stop-dev.sh`，统一本地前后端开发环境的启动与停止方式。

## 2026-05-13 50e46a4 feat: implement phase one MVP

- 搭建 FaceScan 前后端一期 MVP 工程结构。
- 初始化后端 CMake 项目、配置示例、SQLite/文件存储相关基础能力。
- 初始化 Vue/Vite 前端，包含登录、患者基础信息、浏览、拍摄、发送、设置等核心页面。
- 增加患者状态管理、API 客户端、基础样式和导航流程。

## 2026-05-14 cd991d1 feat: refine phase one workflow

- 优化一期业务流程和页面交互细节。
- 扩展后端接口处理能力，补充患者、扫描、设置等流程逻辑。
- 重构顶部步骤、窗口图标和多页面样式，使工作流更贴近桌面应用。
- 更新前端类型、API 调用和页面状态表现。

## 2026-05-15 eb4789a chore: reorganize frontend and backend layout

- 调整前后端目录布局，将配置示例迁移到后端目录。
- 增加后端数据目录占位文件，明确数据库和图片存储位置。
- 更新 README、编辑器配置和忽略规则以匹配新的工程结构。
- 修正设置页配置路径相关展示。

## 2026-05-18 e246287 refactor backend module boundaries

- 将原后端单文件实现拆分为相机、数据库、配置、通用工具和模型等模块。
- 新增文件工具、JSON 工具、时间工具和应用配置封装。
- 建立数据库访问层和相机管理层的头文件/实现文件边界。
- 大幅精简 `main.cpp`，提升后端模块可维护性。

## 2026-05-25 7774246 feat: add point cloud capture workflow

- 增加彩色点云生成算法模块和点云相关数据模型。
- 扩展后端扫描、订单、文件保存和点云处理流程。
- 前端新增点云查看页面，并接入点云预览、拍摄和发送流程。
- 更新设置页、浏览页、拍摄页和 API 类型以支持点云采集工作流。

## 2026-05-26 2c8d2d8 refactor: split backend api and server layers

- 将后端应用 API 逻辑拆分到 `api` 层。
- 新增 HTTP server 层，分离请求监听和业务处理职责。
- 调整相机管理接口和后端构建配置。
- 进一步压缩 `main.cpp`，使入口只负责装配和启动。

## 2026-05-26 2d0e79e test: add backend gtest coverage

- 引入 GoogleTest 后端测试框架。
- 增加 API、相机管理、数据库和 JSON 工具测试。
- 增加测试工具头文件，支持临时配置和测试环境搭建。
- 更新 README/CLAUDE 文档说明测试方式。

## 2026-05-27 760dad5 chore: add vscode backend debug setup

- 增加 VS Code 推荐扩展配置。
- 增加后端构建、测试和运行任务。
- 增加后端调试启动配置。
- 小幅修正 README 和 HTTP server 相关配置。

## 2026-05-27 8157dc1 feat: add Orbbec camera integration

- 引入 Orbbec SDK 头文件和 macOS 动态库。
- 后端构建增加 Orbbec SDK 自动探测和链接配置。
- 扩展相机管理器，支持真实 Orbbec 设备初始化、采集和回退逻辑。
- 更新拍摄接口、前端拍摄页和相机测试以适配真实设备模式。

## 2026-05-28 29a8bb9 docs: document backend and frontend interfaces

- 为后端核心模块、接口、模型和工具函数补充说明注释。
- 为前端 API、类型、组件、路由、状态和页面脚本补充说明注释。
- 更新 README 中的接口和工程说明。
- 提升代码可读性，便于后续维护和交接。

## 2026-05-28 4c443b3 feat: reconstruct point clouds from stereo IR

- 基于双目红外数据扩展点云重建逻辑。
- 更新点云算法模块，补充深度、彩色和坐标重建处理。
- 增加点云 SDK 算法测试覆盖。
- 更新后端构建以纳入新增测试。

## 2026-06-05 d002eb0 feat: show patient thumbnails and order updates

- 患者浏览页新增患者缩略图和订单更新展示能力。
- 后端接口和数据库层增加缩略图/订单更新相关字段查询。
- 扩展前端类型和浏览页样式，优化患者卡片信息展示。
- 补充数据库和 API 测试，调整测试 include 路径。

## 2026-06-05 0ca1626 test: normalize database test includes

- 统一数据库测试文件中的 include 写法。
- 保持测试引用路径与后端测试目录结构一致。

## 2026-06-05 cab359b feat: add camera controls and selection screenshots

- 后端新增相机控制能力和相关 API。
- 扩展相机管理器，支持设备选择、参数控制和截图能力。
- 前端拍摄页增加相机控制、设备/模式选择和截图交互。
- 更新窗口图标、样式、类型定义和后端测试覆盖。

## 2026-06-05 adbd226 feat: add patient date range filter

- 后端患者列表查询支持 `startDate` 和 `endDate` 日期范围参数。
- 前端浏览页将创建日期筛选升级为日期范围弹层。
- 日期筛选支持今天、昨天、近 7/14/30 天、本月、上月和自定义范围。
- 创建/更新患者与发送页加载后刷新患者列表并保持当前患者选中状态。

## 2026-06-05 d11bc57 docs: improve project README

- 将 README 从旧的一期 MVP 说明更新为当前面部扫描工作站项目说明。
- 补充当前功能能力、技术栈、快速启动、前端构建和后端测试步骤。
- 增加后端配置说明，明确 `mock`、`orbbec` 和 `gemini215` 相机模式。
- 整理项目结构、常用页面路由和数据版本控制约定。

## 2026-06-08 5bba863 feat: improve settings and date filter UX

- 浏览页日期范围筛选改为自控弹层，支持外部点击收起并提交日期草稿。
- 设置页增加刷新、错误提示、只读提示、打开数据目录和未保存状态展示。
- 设置页按通用、信息管理、校准、拍摄、三维处理和关于分区展示运行信息。
- 调整页面样式，优化日期筛选、设置卡片、状态标签和工作流页面边界。

## 2026-06-08 1c8d4a9 feat: separate capture and point cloud steps

- 拍摄页将同步采图和点云生成拆分为“同步拍摄”和“下一步”两个动作。
- 四路预览改为按图片加载完成后调度下一帧，减少固定轮询堆积。
- 拍摄页读取当前订单已保存图像数量，只有完整采图后才允许进入点云处理。
- 浏览页搜索和重置增加进行中保护，避免重复触发查询。
